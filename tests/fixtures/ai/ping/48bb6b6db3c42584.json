{
  "note": "Hand-authored to the task schema (no OpenRouter key in the build environment). Re-record with `npm run ai:record` when a key is available.",
  "output": { "ok": true, "echo": "koryograph" }
}
